Some text. #emph[Some emphasised text].

#let afunction = code(some,
   args)

#let otherfunction = code(some,
   args: assigned)

#show: rule(arg,arg2)

#set  heading(numbering:"1.1.")


Some more text
